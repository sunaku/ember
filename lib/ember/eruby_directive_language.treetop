grammar ERubyDirectiveLanguage

  rule directive
    head tail
    /
    head capture tail
    /
    head capture ( !(head capture) !(capture tail) . )* capture tail
  end

  rule head
    '<%'
  end

  rule tail
    '%>'
  end

  rule capture
    [^%]
  end

end
