grammar ERubyDirectiveLanguage

  rule directive
    head tail
    /
    head bind tail
    /
    head bind ( !(head bind) !(bind tail) . )* bind tail
  end

  rule head
    '<%'
  end

  rule tail
    '%>'
  end

  rule bind
    [^%]
  end

end
